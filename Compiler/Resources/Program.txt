func main() as void => {
    var x <- new OptionalInt { some = 4 };    

    for i = 0 to 10 do { };

    var y <- [1, 2, 3, 4, 5, 6];
    @y <- y@[1:5]; 

    ref z <- @y as ref int[];

    void;
};

union OptionalInt {
    none as void;
    some as int;
};

union Token {
    eof         as void;
    open_paren  as void;
    close_paren as void;
    plus        as void;
    minus       as void;
    times       as void;
    int_literal as int;
};