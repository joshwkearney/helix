func test(tok as Token) as int => {
    var y <- match tok
         if int_literal then int_literal + 4
         else 2;

    y;
};

func test3 (tok as Token) as bool => (tok is plus) or (tok is minus);

union Token {
    eof         as void;
    plus        as void;
    minus       as void;
    multiply    as void;
    open_paren  as void;
    close_paren as void;
    int_literal as int;
};

func test1 (x as int) as int => test2(x) + 4;

func test2 (y as int) as int => y*2 + 1;

func binary_search (arr as array[int, ref], to_find as int) as int => {
    0;
};

func selection_sort (arr as array[int, var]) as void => { 
    # Find the smallest element and put it in position i
    for i = 0 to arr.size-1 do {
        var smallest <- arr[i];
        var smallest_index <- i;

        # Find the smallest element
        for j = i + 1 to arr.size-1 do {
            if arr[j] < smallest do {
                @smallest_index <- j;
                @smallest <- arr[j];
            };
        };

        # Swap the smallest with the ith position
        ref swap <- arr[i];

        arr@[i] <- smallest;
        arr@[smallest_index] <- swap;
    };
};