func main () as void => {
    region other {
        func (x as int) => from heap do func (y as int) => x + y;
        void;
    };

    void;
};

func test (x as var int) as void => {
    @x <- 5;
};