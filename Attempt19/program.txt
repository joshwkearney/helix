{
    var x <- 4;
    var meta <- @x;

    var test <- [1, 2, 3];
    test@[0] <- 99;

    var p <- new Point { x = 4, y = 6 };

    some.other.@foo <- 45;

    {
        @meta <- test@[1];
    };

    x;
}