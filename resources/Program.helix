union Option {
	var none as void;
	var some as word;
}

struct Point {
	var x as word;
	var y as word;
}

func test3(var test as bool) as word {
	var p = new Point { 4, 5 };

	if test then {
		p.x = 45;
	}

	var q = p;

	return q.x + q.y;
}

// DONE - Simplify unification
// DONE - Remove anonymous struct and union types
// DONE - Predicate bools
// DONE - Singular structs
// DONE - Make types indexable only by strings
// Singular pointers
// Singular arrays
