union Option {
	var none as void;
	var some as word;
}

func test3(var test as bool) as Option {
	var opt = 45 as Option;

	if test then {
		return 10;
	}

	return opt + 10;
}

// DONE - Simplify unification
// Remove anonymous struct and union types
// Add singular structs and singular arrays
// Make types indexable only by strings
