union IntOption {
	var none as void;
	var some as int;
	var other as bool;
};

func test(var x as IntOption) as IntOption {
	if (x is what) or (x is none) then {
		// Lol
	}
	else {
		// Still works!
		x += 45;
	};

	x;
};