struct Point {
	var x as word;
	var y as Test;
};

struct Test {
	var x as word;
};

func test(var limit as word) as word {
    var i = 0;

	while true {
	    if i < limit then {
	        i += 1;  
	        continue;
	    };
	    
	    return i;
	};

	return i;
};