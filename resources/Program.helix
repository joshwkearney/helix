union IntOption {
	var none as void;
	var some as int;
	var other as bool;
};

struct Point {
	let x as int;
	let y as int;
};

func test() as IntOption {
	var x = new IntOption { some= 45 };

	if (x is other) or (x is none) then {
		// Lol
	}
	else {
		// Still works!
		x += 45;
	};

	x;
};