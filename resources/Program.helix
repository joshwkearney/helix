union Option {
	var none as void;
	var some as word;
}

func test3(var foo as bool) as Option {
	var opt = 45 as Option;
	var test = true;

	if false then {
		test = test and foo;
	}

	if test then {
		return 10;
	}

	return opt + 10;
}

// DONE - Simplify unification
// DONE - Remove anonymous struct and union types
// DONE - Predicate bools
// Singular pointers
// Add singular structs and singular arrays?
// Make types indexable only by strings
