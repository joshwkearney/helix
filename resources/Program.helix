union Option {
	var none as void;
	var some as word;
}

struct Point {
	var x as word;
	var y as word;
}

func test3(var foo as word) as Point {
	var p = new Point { 4, y = foo };

	return p;
}

// DONE - Simplify unification
// DONE - Remove anonymous struct and union types
// DONE - Predicate bools
// DONE - Singular structs
// Make types indexable only by strings
// Singular pointers
// Singular arrays?
