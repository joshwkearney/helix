func test(limit as word) as word {
    var i = 0;

	while true {
	    if i < limit {
	        i += 1;  
	        continue;
	    }
	    
	    break;
	}

	return 99;
}

c_ptr(byte)

&word -> borrowed &word

borrow word[]
borrow &word
borrow 

func test(borrow array as &word) {
    unsafe {
        var x = *(array + 7);
    }
}

&f as (func(word, word) as word);