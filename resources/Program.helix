extern func externalFunc(let x as int) as int;

func loops(var x as int) as int => {
	//var total = 0;

	//for i = 0 to x do {
	//	total = total + i;
	//};

	var total = 0;

	for i = 0 to 15 do {
		total += i;
	};

	total;
};