function main () => void: {
    var test <- void as Option;

    #var copy <- test;

    #var c <- void as int;

    void;
};

union Nullable<T> {
    void null;
    var T data;
};

union Option {
    void none;
    int some;
};

struct OptionSome {
    Point data;

    function is_empty () => bool: false;
};

class Point {
    int x;
    int y;

    function norm () => int: this.x*this.x + this.y*this.y;

    function scale (int factor) => Point: {
        new Point { x = factor * this.x, y = factor * this.y };
    };
};