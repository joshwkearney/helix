{
	let: fib: function:x: {
		if:		add:(int32_equals:(x 0) int32_equals:(x 1)),
		then:	x,
		else:	{			
			let: first:		recurse:(subtract:(x 1)),
			let: second:	recurse:(subtract:(x 2)),

			add:(first second)
		}
	},

	fib:11
}