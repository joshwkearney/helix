extern func test(let x as int) as int;

func test(let x as int) as int => {
	if x > 3 or else 45 < 10
	then 0 
	else 10;
};

func other(let x as int) as int => x + 10;

struct Point {
	let x as int;
	let y as int;
	let z as int;
};