extern func externalFunc(let x as int) as int;

func loops(var x as int) as int => {
	//var total = 0;

	//for i = 0 to x do {
	//	total = total + i;
	//};

	while true do {
		if x > 1500 or else x < 100 then {
			x = x + 10;
		};

		if x > 10 then {
			break;
		};
	};

	x;
};