extern func externalFunc(let x as int) as int;

func test(let x as int) as int => {
	//let a, b = put Point { 4, 5 };
	let t = put Point { 4, 5 };
	let a = t.xx;
	let b = t.y;

	//let x = cast 45 as int;

	let p = put Point;

	if x > 3 and then x < 10
	then 0 
	else 10;
};

func other(let x as int) as int => x + 10;

struct Point {
	let x as int;
	let y as int;
};