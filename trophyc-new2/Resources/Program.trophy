func test(let z as int^) as int => {
	//var x = *z;
	let x = 45;

	while x < 4 do {
		//x = x / 2;
	};

	//void as Point
	//void as int^

	// Comment
	if x > 3 then x else 0;

	// let z = put Point { x= 1, y= 4 };
};

func other(let x as int) as int => x + 10;

struct Point {
	let x as int;
	let y as int;
};

extern func add(let x as int, let y as int) as int;